<h3>
  <%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %>
</h3>
<p><%= portfolio_item.subtitle %></p>
<p>
  <%=  "Technologies: #{portfolio_item.technologies[0].name},
    #{portfolio_item.technologies[1].name},
    #{portfolio_item.technologies[2].name}" if portfolio_item.technologies.count == 3
  %>
</p>
<p><%= portfolio_item.body %></p>
<%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
<br>
<%= link_to "Show the portfolio", portfolio_show_path(portfolio_item) %>
<%= link_to "Edit this portfolio", edit_portfolio_url(portfolio_item.id) %>
<%= link_to 'Delete Portfolio', portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you sure?' } %>
<hr>

<%# If you look at this path for destroy, its the same path as the show page, but its a different action, because we're performing the delete method, i mean, shouldnt it say destroy though? %>
<%# The thing I dont understand about this is still, how does it know that it is looking for not only the specific blog, which is easy cuz we're just mapping over the entire array of blogs, but how does it know to find the blog with the specific id, is that just rails magic? %>
<%# <a href="/portfolios/<%=portfolio_item.id%><%#/edit">edit2</a> %>
