<div class="inner cover">

  <h1 class="cover-heading">Just Now in <i>The Moment</i>!</h1>
  <div class= "row">
    <% @tweets.each do |tweet| %>
      <div class="col-md-6">
        <div class="mindful-news">
          <div class="card">
            <%= tweet %>
            </div>
          </div>
        </div>
        <% end %>
    </div>
</div>
